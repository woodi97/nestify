export interface IMovie {
  id: number;
  title: string;
  year: number;
  genres: string[];
  // description: string;
  // director: string;
  // producer: string;
  // release_date: string;
  // rt_score: number;
}
